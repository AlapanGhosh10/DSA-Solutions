#include<bits/stdc++.h>
using namespace std;

class Solution {
public:
    string convert(string s, int numRows) {
        
    }
};

int main() {
	Solution s;
	string str;
	int row;
	getline(cin, str);
	cin >> row;
	cout << s.convert(str, row);
    return 0;
}